[project]
name = "bitripay"
version = "1.0.0"
description = "BitriPay Gateway API client for Python: payment intents, checkout, QR, refunds, verifications, payouts, settlements, disputes and webhook verification"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
