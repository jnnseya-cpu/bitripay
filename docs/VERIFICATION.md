# Verification report

How every requirement sent for BitriPay is built, where it lives after the backend / frontend / shared split, and
how it is proven. Three layers of proof are used:

| Proof | Command | What it covers |
| --- | --- | --- |
| **Unit and integration tests** | `npm run verify` (CI runs the same) | every API suite under `backend/api/src/tests` against an in-memory ledger (the run prints the current count), plus the shared packages (money math, QR codec, phone normalisation, locale parity, BitriQR signing, Node SDK) and the payout-device protocol tests |
| **Builds and type checks** | `npm run verify` | ESLint over every layer, then shared packages, backend, web, admin, mobile and payout device |
| **Live smoke** | `npm run dev && npm run seed`, then `npm run smoke` | the real servers: public site and positioning copy, user / merchant / agent flows, checkout disclosures, QR analytics, national switch page, every admin console, BitriPay Lite without JavaScript, the partner API (the run prints the current count; last run all checks passed) |

The API test suites (`backend/api/src/tests`) are named after what they prove; the table below maps each requirement
area to them.

## Layer map

| Layer | Path | Talks to |
| --- | --- | --- |
| Backend | `backend/api` | SQLite ledger; publishes `/api/*`, `/api/v1/*` (partner API), `/api/admin/*`, `/lite/*`, USSD/SMS webhooks; imports only `@bitripay/shared` and `@bitripay/bitriqr` |
| Frontend | `frontend/web`, `frontend/admin`, `frontend/mobile`, `frontend/payout-device` | HTTP to the backend only; import `@bitripay/shared` (and `@bitripay/bitriqr` where QR codes are produced on the device) |
| Shared | `shared/core`, `shared/bitriqr`, `shared/sdk-node`, `shared/sdk-php`, `shared/sdk-python` | No dependency on either side |

## Requirements → implementation → proof

### Money, ledger and accounts

| Requirement | Where | Proof |
| --- | --- | --- |
| Immutable double-entry ledger, no direct balance writes, derived balance equals materialised balance | `backend/api/src/services/ledger.ts` (`postTransaction`, `reconcileLedger`) | `core.test.ts` "wallets, transfers & ledger"; `acceptance.test.ts` "ledger under load" (3 000 randomised postings, zero-sum per currency) |
| Wallets in any enabled currency, exchange at disclosed rate and margin | `services/wallets.ts`, `services/transfers.ts` (`exchange`), `services/fx.ts` | `core.test.ts`; `gateway.test.ts` "FX disclosure and route declarations" |
| Ring-fenced holds (disputes, reserves, review, settlement, compliance, savings) that block spending | `services/finops/holds.ts`, ledger `heldOnWallet` guard | `finops.test.ts` "holds and balance classes"; `acceptance.test.ts` savings |
| E-money issuance, safeguarded reserves, headroom, pools, daily reconciliation | `services/emoney.ts` | `emoney.test.ts`; `gateway.test.ts` "e-money issuance" |
| Bank-grade statements (CSV, PDF) | `services/statements.ts` | `statements.test.ts` |
| Idempotency keys on every money POST, hash-chained event log | `middleware/idempotency.ts`, `services/events.ts` | `gateway.test.ts` "ledger, audit and idempotency"; `v1ext.test.ts` |

### Sending, receiving and cross-border

| Requirement | Where | Proof |
| --- | --- | --- |
| Send by @tag / phone / email, QR payment requests, payment links, hosted checkout | `services/transfers.ts`, `services/paymentRequests.ts`, `services/qrcodes.ts`, `frontend/web/src/pages/{Send,Receive,Requests,Checkout}.tsx` | `core.test.ts` "QR & payment requests"; smoke "Send 1.25 USD to bob", "Receive QR", "Point of sale QR" |
| BitriQR: EMVCo merchant-presented QR with signed extension, key registry and rotation, resolver | `shared/bitriqr`, `services/keys.ts`, `services/qrcodes.ts` | `shared/bitriqr` unit tests; `bitriqr.test.ts` "BitriQR payment intents" |
| **Any → any**: fund from wallet, any card, bank or mobile money; deliver to wallet, QR, any bank, any mobile money number, agent, own wallet; recipient chooses the currency received | `services/routing.ts` (`quoteRoute`, `payoutCurrencyOptions`, `createRoute`), web **Move money**, partner API `/v1/routes` | `rails.test.ts` "any → any routing"; `corridor.test.ts` "UK card → Orange Money DRC", "recipient-controlled payout currency"; `v1ext.test.ts` "cross-border routes on the partner API"; smoke "Move money", partner-API quote |
| Remittance to wallet, bank, mobile money or cash pickup in the recipient currency, from the wallet or any card | `services/remittance.ts`, `/v1/remittances`, web **Remittance**, Lite **Send abroad** | `core.test.ts` "withdrawals, agents, remittance"; `v1ext.test.ts`; smoke "Remittance", Lite pages |
| Diaspora-Direct: signed rate policies, four-hour rate cards, institutions, purpose-locked quotes | `services/diaspora.ts` | `intelligence.test.ts` "Diaspora-Direct" |
| Corridors, licences, compliance mode, liquidity, prefunded payout accounts, payout devices, signed SMS evidence | `services/corridors.ts`, `services/liquidity.ts`, `services/payouts.ts`, `services/evidence.ts`, `frontend/payout-device` | `corridor.test.ts`; `gateway.test.ts` "no-API evidence engine"; `onboarding.test.ts` "agent device enrolment"; payout-device protocol tests |
| Withdrawals to bank or mobile money with maker-checker settlement | `services/withdrawals.ts`, `services/verification.ts` | `core.test.ts`; `gateway.test.ts` "maker-checker and administrative step-up" |
| Bulk payouts: CSV validation, four-eyes / step-up approval, in-order execution | `services/bulkPayouts.ts`, `/v1/payouts/batches`, merchant centre tab, admin finops tab | `v1ext.test.ts` bulk payout batch |

### Funding rails and the switch

| Requirement | Where | Proof |
| --- | --- | --- |
| Connector health states HEALTHY / DEGRADED / UNAVAILABLE / MAINTENANCE with capabilities per rail (min / max, refunds, settlement T+n, webhooks, last incident) and a maintenance toggle; provider contract with quote, capabilities, statement parsing, cancel and an idempotency key; probes every minute; Smart Route with settlement speed, FX cost, fraud risk, liquidity and concentration terms, p95 latency, 15-minute buckets, amount band / prefix keys and an uplift metric; sliding-window rate limits keyed by device, MSISDN, QR id and user; correlation ids and audit columns; Guardian "AMBIGUOUS is never credited"; real smart_route and seo_content agents; prompt hash on every run | `services/rails.ts`, `payments/types.ts`, `middleware/rateLimit.ts`, `middleware/correlation.ts`, `services/audit.ts`, `services/guardian.ts`, `services/assist/registry.ts`, `services/assist/runtime.ts` | `contract_platform.test.ts`; `rails.test.ts`; `switch.test.ts` |
| Bitcoin rail adapter (sandbox invoices; BTCPay Greenfield with signed webhooks): Lightning invoice or on-chain address with disclosed rate, expiry and confirmation policy; eligibility by country capability and merchant opt-in only; settlement in BTC or fiat through the ledger; refunds manual; provider operations fields on every gateway | `payments/bitcoin.ts`, `services/capabilities.ts` (`bitcoinEligible`), `services/gateway.ts`, `services/payments.ts`, `frontend/admin/src/pages/Gateways.tsx` | `contract_bitcoin.test.ts` |
| Operations: SLO measurement per route class against the specification targets, switch availability, system health and API operations endpoints, SLA register with measured breaches, gate-to-scale metrics on the go-live checklist, capability-matrix editor, admin System page and Switch national view / configuration / PRA tabs | `middleware/slo.ts`, `services/sla.ts`, `services/goLive.ts`, `routes/admin/system.ts`, `routes/admin/switch.ts`, `frontend/admin/src/pages/{System,Switch,Controls}.tsx` | `contract_operations.test.ts` |
| Pluggable processors (sandbox, Stripe, Paystack, Flutterwave, MTN MoMo, M-Pesa, manual bank / mobile money, open banking), health checks, live / test key modes, 3-D Secure, go-live checklist | `backend/api/src/payments/*`, `services/goLive.ts` | `core.test.ts` "deposits & checkout via sandbox gateway"; `onboarding.test.ts` "processor onboarding"; `openBanking.test.ts` |
| Payment intents and attempts, canonical state machine, failover, AMBIGUOUS suspense, reconciliation | `services/intents.ts`, `services/lifecycle.ts`, `services/rails.ts` | `gateway.test.ts` "payment lifecycle"; `switch.test.ts` L1–L5 |
| National switch (Switch Monétique National): participants, connections, certification, vault, reconciliation | `services/switch/*` | `switch.test.ts` |
| Rail registry, Smart Route scoring, circuit breakers, merchant routing policies | `services/rails.ts` | `switch.test.ts` "rail registry and Smart Route"; `v1ext.test.ts` ranked routes |
| Direct mobile-money rail without operator API (collection numbers, SMS auto-confirm) | `services/momo.ts`, `payments/manualMomo.ts` | `rails.test.ts` "direct mobile money rail" |
| Live rate providers with versioned snapshots, staleness rules | `services/currencies.ts`, `services/fx.ts` | `onboarding.test.ts` "live rate providers" |

### Merchants, gateway API, developers

| Requirement | Where | Proof |
| --- | --- | --- |
| Partner API v1: intents, checkout sessions, links, refunds, payouts, balance, wallets, transfers, remittances, routes, plans / subscriptions, payout batches, agents, credit readiness; scoped keys; `BitriPay-Signature` webhooks with 8 retries / 24 h and replay | `routes/v1.ts`, `routes/v1ext.ts`, `services/webhooks.ts`, `docs/openapi.ts` | `gateway_v1.test.ts` (checkout, webhooks, scoped keys, sandbox simulator); `v1ext.test.ts`; `growth.test.ts` billing |
| Gateway contract: specification webhook events (payment_intent.authorised, verification.confirmed, payout.processing / succeeded / settled, settlement.created / completed, dispute.opened) with delivery notes; idempotency reuse → 409 with endpoint and 24 h expiry; refund lifecycle REQUESTED → APPROVED → PROCESSING → SUCCEEDED / FAILED / REJECTED; REVERSED only through a refund; manual capture and void; fx quotes, payment_resolution, money_requests, transactions/:id; step-up on live keys, rotation and high-value refunds; sanctions screened at cross-border intent creation and asynchronously for domestic intents | `services/webhooks.ts`, `middleware/idempotency.ts`, `services/gateway.ts`, `services/intents.ts`, `routes/v1.ts` | `contract_gateway.test.ts`; `gateway_v1.test.ts`; `gateway.test.ts` |
| Organisations and merchant RBAC: every merchant-class account has an organisation, intents carry organisation and business unit, member roles owner … read_only with a permission matrix enforced on the partner API (cashier refund limit, settlement changes, API secrets, customer exports), business units linked to locations; account types corporate / ngo / government / developer are merchant-class everywhere | `services/organisations.ts`, `routes/organisations.ts`, `middleware/auth.ts`, `shared/core/src/constants.ts` (`MERCHANT_CLASS_ROLES`, `ORG_PERMISSIONS`) | `contract_organisations.test.ts` |
| Embedded checkout SDK (`@bitripay/checkout-js`: iframe embed with an origin-checked postMessage contract, redirect mode, pay button) and the hosted checkout page reporting succeeded / failed / closed to the parent; Shopify via link redirect (start + return); WhatsApp Cloud API channel with signed inbound messages, command grammar, in-thread pay cards, templates and admin settings | `shared/sdk-js`, `frontend/web/src/pages/Checkout.tsx`, `routes/shopify.ts`, `services/channels/whatsapp.ts`, `routes/admin/whatsapp.ts` | `shared/sdk-js/src/checkout.test.ts`; `contract_channels.test.ts` |
| Merchant acceptance score (components, weights, recommendations, daily snapshots); payment graph with ring, mule and duplicate-identity candidates; government QR infrastructure (agencies, services, revenue codes, references, collection dashboard, audit export); smart restricted wallets enforced by a ledger posting policy | `services/acceptanceScore.ts`, `services/paymentGraph.ts`, `services/government.ts`, `services/restrictedWallets.ts`, `services/ledger.ts` (`registerPostingPolicy`), `routes/insights.ts`, `routes/government.ts`, `routes/restricted.ts`, `routes/admin/insights.ts` | `contract_intelligence_layer.test.ts` |
| Scale and safety acceptance: 10 000 concurrent postings leave the ledger zero-sum with every wallet equal to its derived balance; no endpoint lets a client write a balance, a transaction state or an intent state; a wallet payment and a QR payment complete end to end with the agent kill switch on and every allowance at zero | `services/ledger.ts`, `routes/*` | `acceptance_scale.test.ts` |
| Developer portal, OpenAPI 3.1, SDKs (Node, PHP, Python), WooCommerce plugin, generated `docs-api.http`, one sandbox magic-number table shared by the simulator | `frontend/web/src/pages/Developer.tsx`, `shared/sdk-*`, `integrations/woocommerce-bitripay`, `backend/api/src/docs/httpFile.ts`, `backend/api/src/payments/sandbox.ts` | Node SDK tests; `hardening.test.ts` "developer surface", "sandbox magic-number table"; smoke "OpenAPI 3.1 document", "Developer portal" |
| Hosted checkout disclosures (fee, FX rate and margin, receiver currency and amount, total, ETA) with recovery alternatives after a failed attempt; QR centre analytics by day and outcome, downloads and amount rules; resolver reports policy blocks; positioning taglines, trust statement, no-custody wording, Instruction n°58 and RFC citations; TREASURY_SUPER_ADMIN label; "AI paused: ACU depleted" banner; national-switch messages in French and English; default pages upgraded without losing administrator edits | `routes/checkout.ts`, `services/qrcodes.ts`, `content/positioning.ts`, `content/defaults.ts`, `frontend/web/src/pages/{Checkout,QrCentre,SwitchPayments,Assist,Developer}.tsx`, `frontend/admin/src/pages/{Emoney,Users}.tsx` | `contract_ui.test.ts`; smoke checks for taglines, disclosure, analytics, switch page |
| One partner API at `/api/v1` and `/v1` (gateway, switch, financial operations, intelligence, merchant profile); one available-balance rule (holds and frozen wallets) used by balance classes, wallets, savings and forwards | `backend/api/src/app.ts`, `services/finops/holds.ts` `availableBalance` | `hardening.test.ts` "same partner API", "one available-balance rule" |
| Signing-key registry: public `/v1/keys`, administrator listing and step-up revocation with audit | `services/keys.ts`, `routes/admin/index.ts` | `hardening.test.ts` "platform signing keys" |
| Merchant command centre (balance classes, settlement, disputes, fees, bulk payouts, plans, offline kit) and QR centre | `frontend/web/src/pages/{MerchantCentre,QrCentre}.tsx` | smoke merchant section |
| Fees with history, commissions, settlement profiles / cycles / statements, disputes as objects, split payments, processor reconciliation workbench | `services/finops/*` | `finops.test.ts` (7 suites) |
| Settlement profiles with settlement currency and auto-convert through the ledger; statements with provider fee, BitriPay fee and tax as separate lines; split refunds allocated pro rata or absorbed; route stage alias table; §62 domain events; promotional and pending balance classes | `services/finops/settlement.ts`, `services/finops/splits.ts`, `services/finops/fees.ts`, `services/routeLifecycle.ts`, `services/bus.ts`, `services/emoney.ts`, `routes/finops.ts` | `contract_finops.test.ts`; `finops.test.ts`; `emoney.test.ts` |
| Subscriptions and billing: plans, trials, tax, metered usage, mandates, dunning | `services/billing.ts` | `growth.test.ts` "subscriptions and billing"; `openBanking.test.ts` mandate top-up |

### Risk, compliance, identity

| Requirement | Where | Proof |
| --- | --- | --- |
| Raw card numbers only on the sandbox outside production; virtual-card CVV derived and never stored; recovery codes; two-factor enforcement per role with a grace period; KYC documents encrypted at rest; saved remittance recipients under destination-change protection; admin-editable notification templates per channel and language; per-country KYC tier limit seeds; agent float intelligence with a 4-hour horizon and depletion probability, trust score driving agent limits | `services/payments.ts`, `services/virtualCards.ts`, `services/auth.ts`, `middleware/auth.ts`, `services/kyc.ts`, `services/remittance.ts`, `services/notifications.ts`, `services/risk/kycTiers.ts`, `services/risk/agentIntel.ts` | `contract_security.test.ts`; `contract_intelligence_layer.test.ts` |
| Versioned policy engine, fraud score 0–100 with bands, sanctions pre-commit hook, AML monitor, SAR drafts, compliance cases with four-eyes decisions | `services/risk.ts`, `services/risk/*` | `risk.test.ts` (6 suites) |
| KYC tiers 1–4 with per-country limits, KYB, settlement-account change protection, cooling-off, step-up (PIN / passkey / biometrics), 2FA | `services/risk/kycTiers.ts`, `services/risk/accountProtection.ts`, `services/auth.ts`, `services/webauthn.ts` | `risk.test.ts`; `rails.test.ts` "biometric step-up"; `core.test.ts` "config & auth" |
| Error catalogue BP-1xxx … 6xxx on every error | `lib/bpCodes.ts`, `middleware/error.ts` | every test asserting `error.code`; smoke |
| Audit log for admin, KYC and fraud actions; maker-checker on administrative money movements | `services/audit.ts`, `services/verification.ts` | `gateway.test.ts` "maker-checker and administrative step-up", `core.test.ts` "admin" |

### Intelligence and agents

| Requirement | Where | Proof |
| --- | --- | --- |
| One AI gateway with eight layers, ACU metering decoupled from tokens, 66 % gross-margin floor at request, pricing and reconciliation; users never see provider / model / cost; zero-ACU users keep every core flow | `services/assist/gateway.ts`, `services/assist/runtime.ts`, `services/assist/billing.ts` | `intelligence.test.ts` "AI gateway and ACU policy"; `assist.test.ts` "command centres"; `v1ext.test.ts` sanitised runs |
| Agent mesh (RouteOptimiser, FraudScorer, ComplianceMonitor, SavingsAdvisor, CreditReadiness, MerchantGrowth, SupportAgent, LiquidityForecaster, ContentEngine, DisputeResolver, RecipientValidator, Retry Surgeon, Connector Medic, Recon, Exception Hunter, FX Oracle, Rebalancer, Dispute Arbiter, KODA Core), bindings, shadow mode, kill switches, domain events | `services/assist/registry.ts`, `services/assist/bindings.ts`, `services/bus.ts` | `intelligence.test.ts` "agent mesh" |
| Agent float intelligence, trust scores, dynamic commissions, agent-assisted onboarding (Tier 1) | `services/risk/agentIntel.ts` | `risk.test.ts` "agent intelligence" |
| Content engine, blog, SEO, sitemap, IndexNow | `services/blog.ts`, `services/seo*.ts` | `seo.test.ts` |

### Access, offline, locale

| Requirement | Where | Proof |
| --- | --- | --- |
| Offline-signed QR promises (device subkeys, nonces, counters, ceilings, in-order sync, restore on failure, signed receipts) on web, mobile and API | `services/offline.ts`, `frontend/web/src/lib/offline.ts`, `frontend/mobile/src/lib/offline.ts` | `intelligence.test.ts` "offline-signed QR protocol"; `acceptance.test.ts` "offline batch" (50 promises) |
| Offline lifecycle §28: OFFLINE_CREATED → OFFLINE_ACCEPTED_LOCALLY → SYNC_PENDING → ONLINE_VALIDATING → CONFIRMED / REJECTED on every outcome, "Pending confirmation" receipts until the platform confirms, counter-gap evidence per device, encrypted local queues on the phone (XSalsa20-Poly1305, key in the secure store) and on the web (AES-GCM, non-extractable key in IndexedDB) | `services/offline.ts` (`OFFLINE_STATES`, `offlineLifecycle`, `listCounterGaps`), `routes/intelligence.ts` (`/offline/settings`, `/offline/devices/:id/gaps`), `frontend/mobile/src/lib/offline.ts`, `frontend/mobile/src/screens/Offline.tsx`, `frontend/web/src/lib/offline.ts`, `frontend/web/src/pages/Scan.tsx` | `contract_offline.test.ts`; mobile and web type checks |
| Phone app hubs: Pay (scan, send, move, requests, remittance, bills, top-up, offline) and Wallet (balances, add money, withdraw, exchange, agents, savings, cards, statements) as bottom tabs | `frontend/mobile/src/screens/Hubs.tsx`, `frontend/mobile/App.tsx` | mobile type check and lint |
| Launch languages: complete Lingala, Kikongo and Tshiluba packs (every English key, identical placeholders; French before English only for a key added to `en` before its translation lands); any string overridable per deployment from Admin → Languages | `shared/core/src/locales/{ln,kg,lua}.ts`, `LOCALE_FALLBACKS` in `shared/core/src/locales/index.ts` | `shared/core/src/shared.test.ts` (parity enforced for all eleven packs) |
| Money movement on the digital rail without any bank, mobile-money or BTCPay API (blocking checklist item `digital_rail`; optional external rails never block); optional external rails provisioned from the environment: card processors, and the operator-API / BTCPay adapters a deployment contracts, are connectivity-checked at start-up and enabled when the check passes (system event, never on a failed check, `RAILS_AUTO_ENABLE=0` keeps manual activation); go-live checklist covers rails, direct rails, the switch adapter and HTTPS public URLs; `npm run go-live` exits non-zero while a blocking item is open; production deployment for bitripay.com (`deploy/`) | `backend/api/src/payments/index.ts` (`provisionRailsFromEnvironment`), `services/momo.ts` (`provisionDirectRailsFromEnvironment`), `services/goLive.ts`, `backend/api/src/goLive.ts`, `deploy/docker-compose.prod.yml`, `deploy/Caddyfile`, `scripts/deploy.sh` | `contract_provisioning.test.ts`; Docker image builds in CI |
| Production go-live defaults: generated secrets at deploy, sandbox gateway off, live rates from a keyless provider refreshed 6-hourly, KYC before withdrawals, and the official sanctions lists (US OFAC SDN + consolidated, UK OFSI, UN, EU) registered at first start, parsed as published and refreshed daily; every checklist item links to its console screen | `scripts/deploy.sh`, `services/settings.ts`, `services/risk/compliance.ts` (`OFFICIAL_SANCTIONS_SOURCES`, feed parsers), `services/goLive.ts` (`href`), `frontend/admin/src/pages/Controls.tsx` | `sanctions_sources.test.ts` (seeding, four parsers, versioned import, screening, checklist links); live fetch verified on the host |
| Shared-host deployment (nothing on 80/443; localhost ports behind the VPS's existing Nginx / Apache / Caddy, ready-made server blocks; dedicated mode refuses to start over another web server) and the go-live profile: launch records (bank details, currencies, collection numbers, e-money programmes, payout accounts, corridor arrangements, second administrator, SMTP) applied from one JSON document by the go-live command or the console under step-up, idempotent, with the human-only steps reported | `deploy/docker-compose.shared-host.yml`, `deploy/shared-host/*`, `scripts/deploy.sh`, `services/goLiveProfile.ts`, `goLive.ts`, `routes/admin/index.ts` (`POST /api/admin/go-live/profile`), `frontend/admin/src/pages/Controls.tsx` | `go_live_profile.test.ts` (every section from the example file, idempotent re-apply, checklist reflection, console route with step-up and validation) |
| Communication event architecture: one engine, 178 catalogue events in 15 categories fanned out across email, in-app, SMS, push and WhatsApp; 27+ mandatory notices bypass opt-outs; per-user opt-outs by category and channel (Settings → Notifications in the web app); administrator-edited templates win over catalogue text; branded email (site logo, colour, contact footer); every event × channel × recipient logged with status and masked recipient, pruned after 90 days; legacy `notify()` calls with a template key routed through the engine; console page with coverage, template QA (email preview, send test to me) and the delivery log | `services/comms/catalogue.ts`, `services/comms/engine.ts`, `services/notifications.ts` (`insertNotification`, notify hook), `routes/admin/comms.ts`, `routes/account.ts` (preferences), `frontend/admin/src/pages/Comms.tsx`, `frontend/web/src/pages/Settings.tsx` | `comms.test.ts` (catalogue consistency, fan-out and log, opt-outs vs mandatory, legacy routing, console preview and test send) |
| Home-market catalogues: DRC billers (SNEL, REGIDESO, Canal+, DStv, StarTimes, Vodanet, Orange Internet, Liquid Home, DGI), top-up operators (Vodacom, Orange, Airtel, Africell) and CDF vouchers added by name on any database; CDF enabled by default | `seedDefaults.ts` (`ensureDrcCatalogs`), `shared/core/src/money.ts` | `comms.test.ts` "home-market catalogues" |
| USSD sessions, SMS commands, BitriPay Lite without JavaScript, PWA with service worker | `services/channels/*`, `site/lite.ts`, `frontend/web/public/sw.js` | `channels.test.ts` (USSD, SMS commands, Lite web); smoke Lite section |
| Language chain (explicit → device → IP country → default) and currency chain (explicit → wallet → IP country → browser → default), Arabic RTL, launch languages | `services/locale.ts`, `services/cms.ts`, `shared/core/src/locales` | `acceptance.test.ts` "language and currency chains"; smoke "Locale chain" |
| Loud money alerts (push, sound, vibration) with a per-device toggle on web, admin and mobile | `frontend/mobile/src/lib/alerts.ts`, `frontend/web/src/lib/alerts.ts`, `frontend/admin/src/lib/alerts.ts`, `services/notifications.ts` | `channels.test.ts`; mobile type check; smoke settings |
| Every locale pack carries every English key with the same placeholders; shared phone normalisation (E.164 and national significant number) used by accounts, evidence matching and sanctions | `shared/core/src/locales`, `shared/core/src/phone.ts` | `shared/core/src/shared.test.ts` |
| Production refuses development secrets and the sample admin password; compose requires them; settings defaults follow `BASE_CURRENCY`, USSD `*149*01#`, 90 s sessions, 8 webhook attempts within 24 h; every documented settings group is administrator-editable | `backend/api/src/config.ts`, `docker-compose.yml`, `services/settings.ts`, `routes/admin/index.ts` | `hardening.test.ts` "refuses to start", "defaults follow", "edit every documented settings group" |

### Personal finance

| Requirement | Where | Proof |
| --- | --- | --- |
| Savings goals as holds, 10 % anchor (raise-only), round-ups, live-within-means monitor with a plan | `services/savings.ts`, web / mobile **Savings & goals** | `acceptance.test.ts` savings; smoke |
| FX alerts, auto-convert rules with a rate floor, forwards with ring-fenced funds and capped exposure | `services/fxTools.ts`, web **Rates & forwards** | `growth.test.ts` "FX engine tools" |
| Credit readiness signal with explained factors, consented lender access, verified income from linked banks | `services/creditReadiness.ts`, `services/openBanking.ts` | `growth.test.ts` "credit readiness"; `openBanking.test.ts` |
| Open banking: linking under consent, statement import, income verification, pay by bank, VRP mandates | `services/openBanking.ts`, `payments/openBanking.ts`, web **Linked banks** | `openBanking.test.ts` (3 suites) |
| Virtual cards, bills, top-ups, gift cards, P2P exchange with escrow and disputes, referrals, support | `services/{virtualCards,services,p2p,referrals,support}.ts` | `core.test.ts` "services & referrals", "P2P trading" |
| Aggregator perimeter of BCC Instructions n°42 and n°58: one audited click switches every issuer and acquirer function off, the API refuses them (`module_disabled`, authorisation message), acceptance stays on; a merchant QR intent is settled by a switch payment (`intent_id`, one settlement per intent, amount must match) with no ledger entry; a confirmed refund credits the aggregation fee back (art. 23); scheme brand on QR sheets (art. 19) | `services/modules.ts`, `middleware/modules.ts`, `routes/admin/index.ts` (aggregator-perimeter), `services/switch/payments.ts` (`intent_id`, fee credit on SUCCEEDED), `services/switch/fees.ts` (`reverseAggregationFee`, credits at close), `services/switch/settings.ts` (`schemeBrand`), migration `041_switch_fee_reversals.sql`, `frontend/admin/src/pages/Modules.tsx`, `frontend/web/src/pages/QrCentre.tsx` | `aggregator_perimeter.test.ts` (2 tests) |
| Acceptor onboarding inside the aggregator perimeter: KYB dossier filed from the web app with sealed document files (merchant view never carries the file, console dossier view opens it under an audited read), decision under PIN step-up, every merchant-class role may file; settlement account declared with the session (no API key), console list with merchant and institution named, four eyes on activation (`approver_required`); webhook inbox records a signed delivery with both signature checks and flags a tampered body | `services/risk/kycTiers.ts` (`sealKybDocuments`, `openKybDocuments`), `routes/risk.ts`, `routes/admin/risk.ts` (`assertPin`), `services/switch/payments.ts` (`listAllBindings`), `routes/admin/switch.ts`, `services/webhooks.ts` (inbox), `routes/v1.ts` (`/webhook_inbox`), migration `042_webhook_inbox.sql`, `frontend/web/src/components/KybDossier.tsx`, `InstitutionSettlementAccount.tsx`, `frontend/admin/src/pages/Switch.tsx` (Settlement accounts), `Risk.tsx` (dossier) | `kyb_dossier.test.ts` (3 tests), `aggregator_perimeter.test.ts` |
| Coverage by country of the deposit / payment gateways: DRC operators on the direct rail, banks, national switch participants, keyed APIs in scope (sandbox never listed; Kenyan M-Pesa named as such) | `routes/admin/index.ts` (`/gateways/coverage`), `payments/index.ts` (seed names), `frontend/admin/src/pages/Gateways.tsx` | `gateway_coverage.test.ts` (1 test) |
| Payer-institution simulator (scene 3): a point-of-sale sale or QR intent is paid by a simulated payer institution through the switch (consent, payment, dispatch, mirror), the sale turns paid, the aggregation fee accrues, a refund from the console credits it back; no active settlement account or a static QR without amount are refused | `services/switch/payerSimulator.ts`, `routes/admin/switch.ts` (`/simulator/*`), `services/intents.ts` (`paymentRequestId`, request paid on national-switch capture, `payment_intent.created`), `frontend/admin/src/pages/Switch.tsx` (Payer simulator) | `switch_payer_simulator.test.ts` (2 tests), `kyb_dossier.test.ts` (created-intent webhook) |
| Scene 5 controls: KYB approval refused for a listed director with a sanctions case opened; suspension with a reason in the audit trail; Guardian run from the console; incidents register with declaration, acknowledge, resolve and duration | `services/risk/kycTiers.ts` (director screening), `routes/admin/index.ts` (`reason`, `/guardian/run`), `services/switch/connections.ts` (`incidentView`), `routes/admin/switch.ts` (`POST /incidents`), `frontend/admin/src/pages/{Risk,Users,System,Switch}.tsx` | `scene5_controls.test.ts` (3 tests) |
| Demonstration walkthrough defects (found by driving the run-of-show click by click through the web app and console in French): developer portal Webhooks tab crashed on the event catalogue objects; `npm run demo-accounts` and `payout-float` wrote to a database at the caller's working directory instead of `backend/api/data`; transfers were refused by the PIN check before the perimeter (now gated at the mount); go-live checklist labels were English in the French console; the payer simulator result vanished when visiting another tab | `frontend/web/src/pages/Developer.tsx`, `scripts/demo-accounts.sh`, `scripts/payout-float.sh`, `backend/api/src/app.ts`, `frontend/admin/src/pages/{Controls,Switch}.tsx`, `shared/core/src/locales/phrases/fr.ts` | `aggregator_perimeter.test.ts` (module_disabled before PIN), walkthrough `scratchpad/e2e/ros.mjs` (run logs in the audit report) |
| Walkthrough defects, second pass: the Guardian flagged a switch-settled sale as "captured without posting" and halted the platform (invariant now excludes intents settled by a switch payment or a national-switch attempt); the payer simulator returned READY when the standing dispatcher held the outbox lease (it now takes the lease over, fencing token); the platform's own webhook inbox was refused as a destination on http:// deployments; the point of sale defaulted to USD for a DRC acceptor (no simulator pair in USD); the developer portal did not subscribe `payment_intent.created` by default; the incidents register collapsed in a half-width card | `services/guardian.ts`, `services/switch/payerSimulator.ts`, `services/webhooks.ts` (`assertSafeDestination`), `frontend/web/src/pages/{Merchant,Developer}.tsx`, `frontend/admin/src/pages/Switch.tsx` | `switch_payer_simulator.test.ts` (Guardian ok after a switch sale, lease held by another owner) |
| Launch-audit security finding: signing out only dropped the token in the app while the server honoured it for seven days (there was no `/api/auth/logout` route). Every token now carries a session id (JWT `jti`); `POST /api/auth/logout` records it in `revoked_sessions` until the token's own expiry and the authentication middleware refuses it; the web app, the console and the mobile app call it before dropping the token; sign-out-everywhere (`sessions_invalidated_at`) is unchanged; API keys get 400 `no_session` | `lib/jwt.ts`, `middleware/auth.ts` (`isSessionRevoked`, `revokeSession`), `routes/auth.ts`, migration `043_revoked_sessions.sql`, `frontend/{web,admin,mobile}/src/lib/store.tsx` | `auth_logout.test.ts` (2 tests); adversarial re-run: logout 200, reuse 401 |
| Where the customer's money comes from: a customer (signed in, guest on the hosted checkout, or in the mobile app) pays an acceptor's QR, sale or link from the account they hold at their own institution through the national switch — institution list by intent / code / static QR, consent request, switch payment as the acceptor's order, dispatch under the customer's lease owner, institution answer as a state (COMPLETED / REJECTED / PENDING) with customer wording, polling, retry from another institution after a rejection (own order id per attempt), own-code and no-binding refusals, hosted checkout offers the method first; first visit follows the browser language | `services/switch/customerPayment.ts`, `routes/pay.ts` (`/api/pay/institutions`, `/api/pay/institution`, `/api/pay/institution/:id`), `services/paymentRequests.ts` (`checkoutInfo` method `national_switch`), `frontend/web/src/components/InstitutionPay.tsx`, `pages/{Scan,Checkout}.tsx`, `frontend/mobile/src/screens/Pay.tsx`, `frontend/web/src/lib/store.tsx` | `switch_customer_payment.test.ts` (3 tests); browser: scratchpad `e2e/inst.mjs` (scan page, hosted checkout as guest, rejection then retry, no JS errors) and walkthrough scene 3.2 |
| Under the aggregator perimeter the hosted checkout offers only the switch rail: `checkoutInfo` returns `['national_switch']` when the acceptor has an active settlement account and `[]` otherwise (the page explains), and `POST /api/checkout/:code/pay` (card, mobile money, bank, virtual card) and `/wallet` answer 422 `module_disabled` — nothing that would put funds in BitriPay's hands is offered or accepted; the run-of-show carries the answer to hold if the authorisation precedes the switch connection (no real payment, no bilateral route) | `services/paymentRequests.ts` (`checkoutInfo`), `routes/checkout.ts`, `frontend/web/src/pages/Checkout.tsx` | `aggregator_perimeter.test.ts` (hosted checkout under the perimeter) |
| Opening accounts where the SMS provider does not deliver (the DRC among them): `POST /api/admin/users` takes the handle (`tag`) and a transaction `pin`, marks email and phone verified (the administrator vouches) and records it in the audit line, so the console opens a complete customer or acceptor account in one form and the person signs in with the password at once; the registration page keeps the verification code optional and says plainly when it could not be delivered; a keyed SMS provider that refuses, fails or has no credentials queues the message for an enrolled phone instead of losing it (`twilio_to_device`, `africastalking_to_device`, `*_unconfigured_to_device`) | `routes/admin/index.ts` (create user), `services/messaging.ts` (`deviceFallback`), `frontend/admin/src/pages/Users.tsx`, `frontend/web/src/pages/Auth.tsx` | `admin_create_account.test.ts` (2 tests: account verified with tag and PIN, both sign in, sale opened, bad PIN refused; unconfigured Twilio queues for the device) |
| The language choice reaches the data, not only the interface: transaction type labels (every statement row and chart series), the counterparty name of a platform account and a transaction note go through `tr()`; country names come from the runtime in the display language (`setDisplayLanguage`, set next to `setTrLang` in the web app, the console and the phone app) so the 200-odd names need no phrase pack, an unknown code stays as it was given, and a country list that the API ships in English is shown translated (`countryLabel`); the go-live hints of the KYC tiers, the developer key help, the landing navigation, the site footer, the From / To field labels and the Disputes tab are translated | `shared/core/src/countries.ts` (`setDisplayLanguage`, `countryName`), `flags.ts` (`countryLabel`), `frontend/{web,admin,mobile}/src/lib/tr.ts`, 15 transaction-label render sites, 14 country dropdowns, 20 From/To labels | `shared.test.ts` (every transaction type has its French; country names follow the language and fall back); measured in the browser in French: 426 English strings before, 8 after, of which 7 are French words the detector flags wrongly (scratchpad `e2e/lang.mjs`) |
| Nothing depends on an external key except the assistant: bills (rail `bill`) and top-ups (rail `airtime`) are payout instructions on a prefunded SIM of the country, held until the operator's confirmation or maker-checker, `processing` → `completed` / `failed` with the money back on failure; outbound SMS from an enrolled phone's SIM (`sms_outbox`, signed device drain, three retries, console view); the treasury's official reference rate counts as live for go-live | `services/services.ts` (`payBill`, `mobileTopup`, status hook), `services/payouts.ts` (rails, `requeueWaiting`), `services/liquidity.ts` (`anyOperator`), `services/messaging.ts` (outbox), `routes/payouts.ts`, `routes/admin/messaging.ts`, `services/currencies.ts` (`rateFreshness`), `frontend/payout-device` (`sendSms`), migration `040_no_key_fulfilment.sql` | `no_external_keys.test.ts` (5 tests) |
| Regulatory supervision: normalised operations journal (pseudonymised parties, KYC tier, channel, evidence ref) exported as CSV/JSON with a SHA-256 integrity manifest and event-chain state; real-time supervisory report; audit-logged exports | `services/supervision.ts`, `routes/admin/supervision.ts`, admin **Regulatory supervision** | `supervision.test.ts` (4 tests) |
| Published tariff grid: percentage fees per operation (no fixed part except the card issue), amount bands with explicit error codes, agent commission per operation with contractual override, Pay-Link / request-money / card-issue fee types, grossed-up mandate draws, grid applied from the go-live profile | `services/settings.ts` (DEFAULT_FEES), `services/ledger.ts` (`assertAmountBand`, `grossUpForFee`), `services/finops/fees.ts`, `services/goLiveProfile.ts`, admin **Fees, limits & referral** | `tariff.test.ts` (5 tests); fee expectations across `core`, `finops`, `contract_finops`, `risk`, `gateway`, `rails`, `emoney`, `growth` |
| Charts for every user: one geometry engine (`@bitripay/charts`) for bar, column, radar, line, area, gantt, pie, donut, treemap, scatter, histogram, bubble and heatmap charts, rendered as SVG (web, console), react-native-svg (phone) or markup; account and platform analytics series | `shared/charts`, `services/analytics.ts`, web **Insights & charts** (+ dashboard, merchant, agent cards), admin **Analytics & charts** (+ dashboard, supervision), mobile **Insights** | `shared/charts` tests (8); `analytics.test.ts` (2) |
| Public product pages, server-rendered from live platform data: How it works, Industries, Enterprise groups, Developers (API catalogue, scopes, sandbox numbers), Get started, Growth & influencers, All policies, Platform status (+ `/status.json`); footer, header, sitemap, llms.txt and proxy path lists updated | `site/pages.ts`, `routes/site.ts`, `site/render.ts`, web `SiteFooter.tsx`, `deploy/Caddyfile`, `frontend/web/nginx.conf`, `frontend/web/vercel.json` | `seo.test.ts` "public site" |
| Launch audit fixes: sessions invalidated on password change / sign-out everywhere / closure, account closure (right to erasure) for holders and administrators, https-only merchant redirects, credential-less CORS, baseline security headers from the API, amount parse errors as 400, idempotency-key reuse with a different body refused, control characters stripped from notes, markdown link schemes limited, PIN check memo, per-account sign-in throttle; black-box probe script | `services/accountClosure.ts`, `middleware/auth.ts`, `middleware/error.ts`, `lib/http.ts`, `services/ledger.ts`, `services/markdown.ts`, `services/auth.ts`, `routes/auth.ts`, `scripts/launch-audit.mjs`, `docs/audit/LAUNCH-AUDIT.md` | `security_hardening.test.ts` (8 tests); `docs/audit/launch-audit-probes.json` (34 probes) |
| Agent teams: every agent account owns an organisation (kind `agent`, migration 035 backfills existing agents); counter staff are invited by email, phone or @tag, sign in with their own credentials and work at the till on the agent surfaces (`/api/agents/me`, `/api/risk/agents/me`, `/api/payouts/agent`, `/api/insights/float-outlook`) as the agent account while `req.actor` keeps the person: their own PIN confirms, `operatorUserId` / `operatorTag` are recorded on the transaction and the commission entry, and `agent:*` permissions per role are enforced (`org_permission_denied`); the session payload lists memberships; web and mobile tills show the agent's float and a Team tab | `services/organisations.ts` (`onAgentRegistered`, `membershipSummaries`, `resolveMembership` per surface), `middleware/auth.ts`, `routes/agents.ts`, `routes/risk.ts`, `routes/payouts.ts`, `routes/insights.ts`, `services/agents.ts`, `db/migrations/035_agent_organisations.sql`, `shared/core/src/constants.ts` (`ORGANISATION_OWNER_ROLES`, `AGENT_ORG_PERMISSION_KEYS`), `frontend/web/src/components/OrganisationTeam.tsx`, `frontend/web/src/pages/Agent.tsx`, `frontend/mobile/src/screens/Business.tsx` | `agent_team.test.ts` (7 tests) |
| Developer gateway end to end: developer sign-up from the public developers and get-started pages, developer portal open to developers invited into a client organisation, workspace selector (`X-Organisation-Id`) honoured for owner accounts so one developer creates keys and webhooks per client, keys stay the client's after hand-over | `middleware/auth.ts` (`resolveOrganisationContext`, `actAsMember`), `site/pages.ts`, `frontend/web/src/lib/api.ts`, `frontend/web/src/lib/store.tsx`, `frontend/web/src/components/Layout.tsx`, `frontend/web/src/pages/Developer.tsx` | `contract_organisations.test.ts` (multi-client developer), `seo.test.ts` |
| Connected accounts (aggregator / platform API): a platform creates its customers' merchant accounts, acts for them on every v1 operation with `BitriPay-Account`, keeps an application fee paid as a split at capture, receives their events with `account`, hands over with a one-time claim link, loses access when detached by either side; scope and header refusals | `services/platform.ts`, `middleware/auth.ts`, `routes/v1ext.ts` (`/accounts`), `routes/v1.ts` (`application_fee_minor`), `services/webhooks.ts` (fan-out), `routes/auth.ts` (`/claim`), `db/migrations/036_connected_accounts.sql`, `docs/openapi.ts`, `frontend/web/src/pages/Auth.tsx` (`Claim`) | `platform.test.ts` (3 tests) |
| Demonstration test accounts: `npm run demo-accounts` creates customer, merchant and agent with PIN, verified KYC, organisation / team and sandbox balances (USD, CDF) from operator-supplied contacts; idempotent; refused outside sandbox compliance mode | `services/demoAccounts.ts`, `demoAccounts.ts`, `scripts/demo-accounts.sh` | `demo_accounts.test.ts` (3 tests) |
| Main and alternative wallets with flags: every wallet and currency carries its country flag (euro → EU flag, shared currencies a globe); a person picks a main wallet (paid with by default, listed first everywhere) and an alternative, changeable at any time from the web dashboard and the phone settings (`PUT /api/wallets/preferences`) | `shared/core/src/flags.ts`, `services/wallets.ts` (`walletRole`, `listWalletsOrdered`, `setWalletPreferences`), `routes/wallets.ts`, `db/migrations/037_wallet_preferences.sql`, `frontend/web/src/pages/Dashboard.tsx`, `frontend/mobile/src/screens/Account.tsx` | `wallet_preferences.test.ts` (2 tests) |
| SMS providers: Africa's Talking (live and sandbox hosts, per-recipient status, optional sender ID) next to Twilio, selected by `SMS_PROVIDER` or the console's Messaging → SMS settings; channel readiness names the provider in force | `services/messaging.ts` (`smsProvider`, `africasTalkingUrl`), `services/comms/engine.ts`, `config.ts`, `frontend/admin/src/pages/Messaging.tsx`, `deploy/.env.production.example` | `sms_providers.test.ts` (3 tests) |
| P2P marketplace end to end: sell ad refused without cover, ad published and listed, own-ad and out-of-range offers refused, wallet-paid trade settles both legs atomically (seller pays the P2P fee in the sold currency, buyer pays the price from the price-currency wallet), external payment holds escrow → paid → PIN release, counter-offers supersede and the offering party cannot accept their own offer, buyer cancellation refunds escrow and restores the ad, administrator resolves a dispute by refund or release | `services/p2p.ts`, `routes/p2p.ts`, `routes/admin/index.ts` (`/p2p/*`), `frontend/web/src/pages/P2P.tsx` | `p2p.test.ts` (6 tests) |
| Public site on phones: the landing header and every server-rendered page (About, Developers, Legal, Blog…) show a ☰ menu below 720 px / 640 px listing How it works, Developers, Blog, About, Fees, Sign in and Open an account (the server-rendered menu is a `<details>` element and needs no JavaScript); the signed-in shell keeps its ☰ sidebar button and now clears the phone status bar when installed as a PWA | `frontend/web/src/pages/Landing.tsx`, `frontend/web/src/landing.css`, `backend/api/src/site/render.ts`, `frontend/web/src/theme.css` | screenshots at 390 px (landing menu, Developers page menu) |
| Screen fit on phones and tablets: every signed-in web page and console page measured at 390 px and 820 px with no horizontal overflow (cards, key-value rows, codes, pill tabs, amount groups and inline-width inputs wrap; single-column grids use `minmax(0, 1fr)`; `grid-column: span` resets on one column) | `frontend/web/src/theme.css`, `frontend/admin/src/theme.css` | Playwright sweep of 112 page/size pairs, 0 overflowing |
| Move money → mobile money: choosing a country keeps the country selected and narrows the operator list to that country (two state updates from the picker no longer overwrite each other) | `frontend/web/src/pages/MoveMoney.tsx` | browser check: country CD → 4 operators, all CD |
| Virtual cards are loaded only from the wallet of the same currency (issue with first load, fund) and unloaded back to it; an empty wallet is refused with `insufficient_funds`; the fund dialog names the source wallet and its available balance and refuses more than it holds | `services/virtualCards.ts`, `frontend/web/src/pages/VirtualCards.tsx` | API check against an empty wallet (issue → `insufficient_funds`, fund → `insufficient_funds`) |
| Console balance adjustment is a maker-checker proposal: the Users page says where it waits (Verification console → "Awaiting a second approver") and that a different administrator approves it under PIN step-up | `frontend/admin/src/pages/Users.tsx`, `routes/admin/index.ts` (`/users/:id/adjust`), `services/verification.ts` | existing maker-checker tests |
| Blank pages after Scan & pay: leaving the scanner threw synchronously from html5-qrcode ("Cannot stop, scanner is not running or paused") inside an effect cleanup, which unmounted the whole React tree, so every later page was blank until a refresh. Teardown is guarded by scanner state, and both apps carry an error boundary at the root and around page content (reset on navigation) that shows the error with Reload / Try again instead of a white screen | `frontend/web/src/components/Scanner.tsx`, `frontend/web/src/components/ErrorBoundary.tsx`, `frontend/admin/src/components/ErrorBoundary.tsx`, both `Layout.tsx` and `main.tsx` | click-through of all 35 sidebar items at 390 px and 1280 px: 0 blank, 0 errors |
| Statement verification link opens a readable page in a browser (number, period, balances, entry count, SHA-256, generated date, holder reference; never the holder's name or transactions) and a 404 page for unknown numbers; API clients still receive JSON | `site/render.ts` (`statementVerifyPage`), `routes/public.ts` | `statements.test.ts` (HTML and JSON branches) |
| Installable PWA on phones and tablets: PNG icons (192, 512, maskable 512, Apple touch 180) next to the SVG, manifest `id`, Apple status-bar and capable metas; the service worker keeps them with the shell | `frontend/web/public/manifest.webmanifest`, `frontend/web/public/icons/*`, `frontend/web/index.html`, `frontend/web/public/sw.js` | manifest lints against the installability rules (192 + 512 PNG, maskable) |
| Delete account (right to erasure) for every account holder except administrators: Settings → Security on the web and Account → Security on the phone show the blockers (balances, holds, pending, card money, open cash requests), ask for password, PIN and the word CLOSE, then sign the person out; the API refuses administrator self-deletion (`admin_account`) | `routes/account.ts` (`DELETE /api/account`, `GET /api/account/closure`), `services/accountClosure.ts`, `frontend/web/src/pages/Settings.tsx` (`DeleteAccount`), `frontend/mobile/src/screens/Account.tsx` | `security_hardening.test.ts` (blocked, wrong PIN, admin refused, closed and anonymised) |
| Transaction and P2P references never collide: the reference is regenerated until unused inside the posting transaction (the scale test posted 10 000 transfers and once hit a duplicate six-symbol reference) | `services/ledger.ts`, `services/p2p.ts` | `acceptance_scale.test.ts` |
| Demonstration payout float: `npm run payout-float -- --operator orange_cd --msisdn +243… --amount 1000000` creates (or reuses) the payout account for that operator SIM, prefunds it and re-queues every transfer waiting for liquidity on that operator; SIM and amount validated; sandbox compliance mode only | `services/payoutFloat.ts`, `payoutFloat.ts`, `scripts/payout-float.sh` | `payout_float.test.ts` (2 tests) |
| National switch stays with BitriPay: the merchant app no longer lists "National switch" and the page answers merchants with where their bank and mobile money payments appear; the switch connection, its states and its interchange are operated by the platform under the aggregator licence (console → Switch) | `frontend/web/src/components/Layout.tsx`, `frontend/web/src/pages/SwitchPayments.tsx` | web typecheck and build |
| Point of sale with items and VAT: description lines with quantity and unit price, VAT added at the merchant's default rate (gateway settings, DRC 16 %) or a per-sale rate, subtotal / VAT / total computed by the API from the lines (never trusted from the client), the QR and the payment carry exactly the total, the itemised receipt (with the merchant's tax identifier) shows on the POS, the pay page and the transaction receipt and prints; phone POS gets the same lines | `services/paymentRequests.ts` (`buildSale`, `saleOf`), `routes/paymentRequests.ts`, `routes/merchant.ts`, `services/users.ts` (`vatRate`, `taxId`), `shared/core/src/types.ts` (`Sale`, `SaleItem`), `frontend/web/src/pages/Merchant.tsx` (`MerchantPos`, `SaleReceipt`), `Checkout.tsx`, `Transactions.tsx`, `frontend/mobile/src/screens/Business.tsx` | `pos_sale.test.ts` (2 tests); phone screenshot of a two-line sale at 16 % |
| The whole web app translates, not only headings: 1 076 English phrases (JSX text, text props, option labels, toasts, conditional strings) extracted by `scripts/i18n-extract.mjs` into `tr('…')` calls and a generated catalogue; French pack hand-written for every phrase (placeholders checked); Lingala, Kikongo and Tshiluba fall back to French; the page subtree re-renders on language or override change | `frontend/web/src/lib/tr.ts`, `lib/i18n.ts`, `components/Layout.tsx`, `shared/core/src/locales/phrases/{catalogue,fr}.ts`, `shared/core/src/locales/index.ts` | `shared.test.ts` (French covers the catalogue, placeholders, no accidental English), web typecheck and build |
| Translation engine for the other languages: console → Languages shows phrase coverage per language and "Translate missing phrases" runs the platform model (content-agent key and model) on what is missing only, keeps placeholders and product names or refuses the phrase, stores results as overrides editable by hand, audited | `services/translationEngine.ts`, `routes/admin/index.ts` (`/translations/:lang/status`, `/translations/:lang/translate`), `frontend/admin/src/pages/Languages.tsx` | `translation_engine.test.ts` (3 tests, injected translator) |
| Aggregation fee invoicing on national-switch payments: a confirmed credit accrues the merchant's fee (`switch_payment`, 0.80 % by default, contract overrides) as a journal fact and an entry; an administrator closes a past period into one invoice per merchant and currency; the merchant sees fees and invoices in Merchant centre → Fees and pays from the wallet with the PIN; the administrator records a bank payment or voids; the fee never touches the interbank flow | `services/switch/fees.ts`, `services/switch/payments.ts`, `routes/switch.ts`, `routes/admin/switch.ts`, `db/migrations/038_switch_fees.sql`, `frontend/web/src/pages/MerchantCentre.tsx`, `frontend/admin/src/pages/Switch.tsx` (Fees tab) | `switch_fees.test.ts` (2 tests); `switch.test.ts` journal now ends with `AGGREGATION_FEE` |
| Regulator documents: the BCC deck and the demonstration run-of-show render to branded A4 PDFs (logo cover, numbered header and footer, mermaid diagrams rendered with legible sequence settings); the version handed to the BCC is generated from the working deck by removing every `data-internal` passage and filling the company fields from the command line, which refuses to finish silently while a field is still « à compléter » | `scripts/render-pdf.mjs` (`renderPdf`, `renderDiagrams`), `scripts/regulator-submission.mjs`, `docs/regulator/*.html`, `docs/regulator/pdf/*.pdf` | print-mode screenshots of the cover, the switch slide and the last slide of the submission; `grep data-internal` on the submission finds only the stylesheet rule |
| Profile and cover pictures for every account type (personal, merchant-class, agent, administrator) with autosave: choosing a file uploads it at once (shrunk on the device: 512 px photo, 1600 × 600 cover), the API checks the magic bytes and size, stores the bytes apart from the users row and serves them from a public versioned URL with a year of caching; photo shown wherever an account appears (recipient lookup, checkout, receipts, console); owner removal, administrator removal (audited) and removal on account closure; phone app picks from the photo library | `db/migrations/039_profile_pictures.sql`, `services/pictures.ts`, `routes/account.ts` (`PUT/DELETE /api/account/picture/:kind`), `routes/public.ts` (`GET /api/pictures/:userId/:kind`), `routes/admin/index.ts` (`DELETE /api/admin/users/:id/picture/:kind`), `frontend/web/src/components/ProfilePictures.tsx`, `frontend/admin/src/components/ProfilePictures.tsx`, `frontend/admin/src/pages/Users.tsx`, `frontend/mobile/src/screens/Account.tsx` (expo-image-picker) | `pictures.test.ts` (3 tests); web, admin and mobile typecheck |
| French everywhere, not only headings: the phrase layer now covers the phone app (screens, alerts, buttons), the console (every page, table head, tab, navigation, language selector in the top bar) and the API (every error sentence, notification title and communication event subject/body): the three clients translate API messages the moment they arrive, the server translates notifications and communication events in the account's language before sending; one catalogue (4 096 phrases) and a hand-written French pack that covers it; module-level tables (navigation, step labels) translate at render | `scripts/i18n-extract.mjs` (web, mobile, admin, API scan), `frontend/mobile/src/lib/{tr,i18n}.ts`, `frontend/admin/src/lib/{tr,i18n}.ts`, `frontend/*/src/lib/api.ts`, `frontend/admin/src/lib/store.tsx`, `frontend/admin/src/components/Layout.tsx`, `backend/api/src/services/notifications.ts`, `services/comms/engine.ts`, `shared/core/src/locales/phrases/{catalogue,fr}.ts` | `shared.test.ts` (French covers the whole catalogue, placeholders kept); `comms.test.ts`; console screenshots in French (users, KYC); web, admin and mobile typecheck |
| Sanctions name matching on whole words (any order; single-word entries of five letters or more), never substrings; phone numbers need eight digits | `services/risk.ts` (`nameMatchesEntry`), `services/users.ts` | `sanctions_sources.test.ts` |

## Known limits (stated, not hidden)

- Live processor, operator, open-banking and rate-provider credentials are not bundled; every integration runs
  end to end on its sandbox or evidence rail, and switching a corridor to `live` is an administrator decision
  under step-up.
- The mobile and payout-device apps are type-checked and their protocol code is tested against Node's crypto; they
  are not driven on a device in CI.
- Acceptance item 12 of the operating-system contract (p95 latency, Lighthouse on low-end Android) is an
  operational measurement to take on the deployed stack, not a unit test.
